[alias]
    co =                checkout
    st =                status
    ci =                commit
    br =                branch
    cp =                cherry-pick
    purge =             clean -fdx
    unstage =           restore --staged
    amend =             ci --amend
    publish =           push --set-upstream origin head
    rst =               reset --hard HEAD
    latest =            describe --abbrev=0
    alias =             config --get-regexp alias
    sorted-authors =    "!$DOTGIT/list-authors"
    pp =                "!$DOTGIT/pull-push"
    sa =                sorted-authors
    list-authors =      shortlog -e -s -n
    la =                list-authors
    count-authors =     shortlog -s -n
    ca =                count-authors
    pretty-log =        log --pretty=format:'commit: %C(bold red)%h%Creset %C(red)<%H>%Creset %C(bold magenta)%d %Creset%ndate: %C(bold yellow)%cd %Creset%C(yellow)%cr%Creset%nauthor: %C(bold blue)%an%Creset %C(blue)<%ae>%Creset%n%C(cyan)%s%n%Creset'
    pl =                pretty-log
    log-no-merges =     log --no-merges --first-parent
    pl-no-merges =      pl --no-merges --first-parent
    reset-to-server =   "!$DOTGIT/reset-to-server"
    ui =                "!$DOTGIT/git-ui.zsh"
    delete-remote =     push origin --delete
    fco =               "!$DOTGIT/fuzzy-checkout"
    update =            "!$DOTGIT/fetch-and-merge-origin-main"
    skip-ci =           push -o ci.skip
    current-branch =    rev-parse --abbrev-ref head
[pull]
    rebase = false
[fetch]
    prune = true
[init]
    defaultBranch = main
[rebase]
    autosquash = true
[branch]
    sort = committerdate
# git-delta
[core]
    pager = delta
    excludesfile = /Users/nickka/.gitignore_global
    editor = nvim
[interactive]
    diffFilter = delta --color-only
[delta]
    navigate = true  # use n and N to move between diff sections
    light = false
    line-numbers = true
[merge]
    conflictstyle = diff3
    autoStash = true
[diff]
    colorMoved = zebra
    tool = Kaleidoscope
[pager]
    branch = false
[user]
    name = Nick Kaczmarek
    initials = nk
[commit]
    template = /Users/nickka/.gitmessage
    gpgsign = true
[tag]
    gpgsign = true
[gpg]
    program = /opt/homebrew/bin/gpg
    format = openpgp
[filter "lfs"]
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
    clean = git-lfs clean -- %f
[includeIf "gitdir:/Users/kerams/Developer/"]
    path = ~/.gitconfig-personal
[includeIf "gitdir:/Users/kerams/Desktop/"]
    path = ~/.gitconfig-personal
[includeIf "gitdir:/Users/kerams/Documents/"]
    path = ~/.gitconfig-personal
[includeIf "gitdir:/Users/nickka/work/"]
    path = ~/.gitconfig-work

[includeIf "gitdir:/Users/nickka/Developer/"]
    path = ~/.gitconfig-personal-work
[includeIf "gitdir:/Users/nickka/Desktop/"]
    path = ~/.gitconfig-personal-work
[includeIf "gitdir:/Users/nickka/Documents/"]
    path = ~/.gitconfig-personal-work

